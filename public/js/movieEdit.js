console.log('movieEdit-js success')

const qs = (element) => document.querySelector(element);
const qsa = (element) => document.querySelectorAll(element);

let seccion = qs('section');



seccion.classList.add("fondoCRUD")
